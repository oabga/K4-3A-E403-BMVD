# Đã chuyển sang `codebase/`

Thư mục nộp chuẩn: **`../codebase/`**.

Chạy:
```bash
python codebase/server.py
```

Nếu thư mục `prototype/` còn trên máy vì Windows đang khóa file, có thể xóa tay sau khi tắt server/Cursor lock.
