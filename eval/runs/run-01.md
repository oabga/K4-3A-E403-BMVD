# Lượt đầu CP3 · run-01

**Chưa chạy AI thật trên máy này** (thiếu `.env` key). Điền sau:

```bash
cp .env.example .env   # OPENAI_API_KEY hoặc GEMINI_API_KEY
python3 prototype/run_eval.py
```

File này sẽ bị ghi đè thành bảng đạt/không + nhóm lỗi.

- Model: (chưa)
- Prompt: `eval/prompt.md`
- Fetch/retry: MOCK
- Số mẫu: 15 (10 thường + 5 khó)
- Đạt: — / 15 = — %
- Sai nguồn: — · Không hỏi lại: — · Lỗi kỹ thuật: —

Quality bar: **chưa khóa** (CP4).
